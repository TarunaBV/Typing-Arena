# config.py

WIDTH = 800
HEIGHT = 600
FPS = 60
FONT_SIZE = 32
WORD_LIST = [
    "print", "while", "elif", "class", "import", "return", "lambda", "break",
    "continue", "global", "def", "yield", "assert", "raise", "try", "except"
]
SPAWN_INTERVAL = 1500  # milliseconds
